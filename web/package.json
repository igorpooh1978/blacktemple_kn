{
  "name": "blacktemple-kn-web",
  "private": true,
  "version": "0.1.0-dev",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "size": "node ./scripts/size-gate.mjs"
  },
  "dependencies": {
    "preact": "^10.26.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "^5.8.2",
    "vite": "^5.4.14",
    "vitest": "^2.1.9"
  }
}
