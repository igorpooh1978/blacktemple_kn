#requires -Version 5.1
Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"
Write-Host "release.ps1: GitHub Releases packaging is not implemented this wave."
Write-Host "Build IPK with .\build.ps1 then attach artifacts manually after hardware PASS."
exit 2
