# Local research (not in git)

`.research-local/` is gitignored.

Place the reference APK there (or keep it in Downloads). Never commit:

- `*.apk` / `*.aab`
- decompiled trees
- extracted assets / fonts / Rive
- token dumps
- real BlackKey

Public conclusions live in `docs/research/`.
